VITE_APP_ENV=development
VITE_API_BASE_URL=http://localhost:3001
# Set to "false" to use the live API instead of mock data
VITE_USE_MOCK_CATALOG=true
# Set to "false" to route orders through the real API instead of the mock
VITE_USE_MOCK_ORDER=true
